package com.qubee.messenger.ui.contacts

import androidx.lifecycle.ViewModel

// Pre-alpha placeholder — the real ContactsViewModel landed when the
// data layer (Room + Repositories) is wired up. See ContactsFragment
// for context.

class ContactsViewModel : ViewModel()

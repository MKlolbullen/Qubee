<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- The P2P node is a long-lived connection to peer devices, not a
         time-boxed data transfer. `dataSync` is force-stopped after
         ~6h/24h on Android 14, which would kill background delivery;
         `connectedDevice` matches an ongoing peer connection and isn't
         timeout-capped. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- CAMERA backs the QR scanner. RECORD_AUDIO was requested for an
         audio-note feature that isn't implemented; dropped until it
         ships (over-broad permissions are a review/privacy flag). -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".QubeeApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Qubee"
        tools:targetApi="34">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Qubee"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- qubee://invite/<token>  and  qubee://identity/<token> -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="qubee" android:host="invite" />
                <data android:scheme="qubee" android:host="identity" />
            </intent-filter>
        </activity>

        <!-- Embedded ZXing QR scanner. Declared so the camera capture
             activity can run inside our task with our theme. -->
        <activity
            android:name=".ui.invite.QrScannerActivity"
            android:screenOrientation="fullSensor"
            android:stateNotNeeded="true"
            android:theme="@style/Theme.AppCompat"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <!-- OOB identity verification: fingerprint compare, scan-peer-QR,
             and SAS. Not exported — only launched from within the app. -->
        <activity
            android:name=".ui.contacts.verification.ContactVerificationActivity"
            android:exported="false"
            android:theme="@style/Theme.Qubee" />

        <service
            android:name=".service.MessageService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

    </application>

</manifest>
